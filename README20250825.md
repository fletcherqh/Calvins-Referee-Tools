# Patch: Output Log override v1.1 (exact spacing)

Fix: ensures *exactly one* blank line between entries even if a button adds its own newline.

## Install
1) Replace your previous `output-override.js` in `/js/` with this one.
2) Keep the same `<script src="js/output-override.js"></script>` near the end of `test_main.html`.

## Test
- Press `stats`, `reaction`, `loyalty`, then `Fighter` → spacing should be consistent: one blank line between each block.
